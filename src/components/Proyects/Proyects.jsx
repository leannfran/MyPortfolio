import React from 'react';

export const Proyects = ({item}) => {
    return (
        <div className="w-[20rem] flex flex-col">
            <h4>{item.nombre}</h4>
            
        </div>
    );
}

